import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sadeezanvakti.app',
  appName: 'Ezan Vakti: Namaz (Diyanet)',
  webDir: 'dist'
};

export default config;
